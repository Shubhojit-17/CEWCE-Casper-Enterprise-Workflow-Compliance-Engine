# =============================================================================
# Railway Production Environment Variables
# =============================================================================
# Copy these to your Railway service environment variables
# =============================================================================

# ==================== BACKEND SERVICE ====================
# These are set in the Railway Backend service

# Database (Railway provides this automatically when you add PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/database

# Redis (Railway provides this automatically when you add Redis)
REDIS_URL=redis://default:password@host:port

# JWT Secret (generate a random 32+ character string)
JWT_SECRET=your-super-secret-jwt-key-at-least-32-chars

# CORS Origins (comma-separated list of allowed origins)
# Replace with your actual Railway frontend URL
CORS_ORIGIN=https://your-frontend.up.railway.app

# Casper Network Configuration
CASPER_NODE_URL=https://rpc.testnet.casperlabs.io/rpc
CASPER_CHAIN_NAME=casper-test

# Optional: Contract hashes (if deployed)
# WORKFLOW_CONTRACT_HASH=hash-xxx
# WORKFLOW_CONTRACT_PACKAGE_HASH=hash-xxx

# Logging level
LOG_LEVEL=info
NODE_ENV=production


# ==================== FRONTEND SERVICE ====================
# These are set in the Railway Frontend service as build arguments

# Backend API URL (the public URL of your Railway backend service)
# IMPORTANT: Include /api/v1 path
VITE_API_URL=https://your-backend.up.railway.app/api/v1

# Casper Network Configuration
VITE_CASPER_NODE_URL=https://rpc.testnet.casperlabs.io/rpc
VITE_CASPER_CHAIN_NAME=casper-test
VITE_CASPER_EXPLORER_URL=https://testnet.cspr.live

# Optional: Contract hashes (if deployed)
# VITE_WORKFLOW_CONTRACT_HASH=hash-xxx
# VITE_WORKFLOW_CONTRACT_PACKAGE_HASH=hash-xxx
